var Paper2SlideGenerator = {
    /**
     * Get style template
     */
    getStyleTemplate(style) {
        const styles = {
            'modern': `
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }
        .slide {
            background: linear-gradient(145deg, #ffffff 0%, #f8f9fa 100%);
            width: min(900px, 95vw);
            min-height: 500px;
            margin: 30px auto;
            padding: 50px 60px;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            position: relative;
        }
        .slide::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 6px;
            background: linear-gradient(90deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            border-radius: 16px 16px 0 0;
        }
        h2 {
            color: #2d3748;
            font-size: 2em;
            font-weight: 700;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 3px solid #667eea;
        }
        .content { font-size: 1.2em; line-height: 1.8; color: #4a5568; }
        ul, ol { margin-left: 25px; margin-top: 15px; }
        li { margin-bottom: 12px; }
        @media print {
            body { background: white; padding: 0; }
            .slide { box-shadow: none; page-break-after: always; margin: 0; border: 1px solid #ddd; }
        }`,

            'minimal': `
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Inter', 'Helvetica Neue', sans-serif;
            background: #fafafa;
            padding: 20px;
        }
        .slide {
            background: white;
            max-width: 900px;
            margin: 20px auto;
            padding: 60px;
            border: 1px solid #e5e5e5;
        }
        h2 {
            color: #111;
            font-size: 1.8em;
            font-weight: 600;
            margin-bottom: 30px;
        }
        .content { font-size: 1.1em; line-height: 1.9; color: #333; }
        ul, ol { margin-left: 20px; margin-top: 20px; }
        li { margin-bottom: 15px; }`,

            'dark': `
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'SF Pro Display', -apple-system, sans-serif;
            background: #0d1117;
            padding: 40px 20px;
        }
        .slide {
            background: linear-gradient(180deg, #161b22 0%, #21262d 100%);
            max-width: 900px;
            margin: 30px auto;
            padding: 50px 60px;
            border-radius: 12px;
            border: 1px solid #30363d;
        }
        h2 {
            color: #58a6ff;
            font-size: 1.9em;
            font-weight: 600;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #30363d;
        }
        .content { font-size: 1.15em; line-height: 1.8; color: #c9d1d9; }
        ul, ol { margin-left: 25px; margin-top: 15px; }
        li { margin-bottom: 12px; color: #8b949e; }`,

            'academic': `
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Times New Roman', Georgia, serif;
            background: #f5f5f5;
            padding: 30px;
        }
        .slide {
            background: white;
            max-width: 850px;
            margin: 25px auto;
            padding: 50px;
            border: 2px solid #333;
        }
        h2 {
            color: #1a1a1a;
            font-size: 1.7em;
            font-weight: bold;
            margin-bottom: 25px;
            text-align: center;
            border-bottom: 1px solid #666;
            padding-bottom: 15px;
        }
        .content { font-size: 1.1em; line-height: 1.7; color: #222; text-align: justify; }
        ul, ol { margin-left: 30px; margin-top: 20px; }
        li { margin-bottom: 10px; }`
        };
        return styles[style] || styles['modern'];
    },

    /**
     * Wraps LLM-generated slide HTML with full document structure
     * @param {string} slidesHTML - HTML content generated by LLM (section tags)
     * @param {string} title - Document title
     * @param {string} style - Style template name
     * @returns {string} - Complete HTML document
     */
    wrapHTML(slidesHTML, title, style) {
        const styleCSS = this.getStyleTemplate(style || 'modern');

        return `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${this.escapeHtml(title)} - Slides</title>
    <style>${styleCSS}</style>
</head>
<body>
${slidesHTML}
</body>
</html>`;
    },

    /**
     * Legacy method for non-LLM generation
     */
    generateHTML(slides, title) {
        const slideItems = slides.map((s, i) => `
            <section class="slide" id="slide-${i}">
                <h2>${this.escapeHtml(s.title)}</h2>
                <div class="content">${this.escapeHtml(s.content)}</div>
            </section>
        `).join('\n');

        return this.wrapHTML(slideItems, title || 'Presentation', 'modern');
    },

    escapeHtml(text) {
        const map = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' };
        return String(text).replace(/[&<>"']/g, s => map[s]);
    }
};
